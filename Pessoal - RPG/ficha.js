function sistema(){
    /*
    guardar os valores de atributos: Agilidade, Força, Intelecto, Presença e Vigor
    Colocar uma forma de identificar qual classe a pessoa escolheu
    Definir os valores de vida, sanidade e pontos de esforço baseado na classe que a pessoa escolheu e nos atributos de vigor e presença
    */

    const agilidade = document.getElementById('agilidade').value;
    const forca = document.getElementById('forca').value;
    const intelecto = document.getElementById('intelecto').value;
    const presenca = document.getElementById('presenca').value;
    const vigor = document.getElementById('vigor').value;

    const classe = document.getElementById('classe-select');
    /* Dependendo da classe que o usuário escolher, o cálculo dos stats vai ser diferente */
};

sistema()
